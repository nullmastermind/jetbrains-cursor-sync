{
  "name": "cursor-sync",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build --compile --minify --bytecode ./index.ts --outfile dist/cursorsync --target bun",
    "run:window": "bun run autoit.ts"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/bun": "latest",
    "@types/node": "^22.7.4",
    "prettier": "^3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.7.7"
  }
}
